name: 🚀 Deploy website on push

on: 
  push:
    branches: [main]


  workflow_dispatch:
  
jobs:
  ftp-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ftp.f60k.com
        username: zhg72lxr84b1
        password: ${{ secrets.ftp_password }}
        server-dir: /home/zhg72lxr84b1/domains/test.hergo.jp/html/
        protocol: ftps
        port: 22
